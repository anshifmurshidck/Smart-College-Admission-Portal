const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch({ headless: 'new' }); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('response', async res => { if (res.url().includes('supabase')) { try { const text = await res.text(); console.log('SUPABASE RESP:', text); } catch(e){} } }); await page.goto('http://localhost:5173/', { waitUntil: 'networkidle2' }); console.log('Navigated to /'); await page.evaluate(() => { window.history.pushState({}, '', '/admin/students'); window.dispatchEvent(new Event('popstate')); }); console.log('Pushed state to /admin/students'); await new Promise(r => setTimeout(r, 3000)); await browser.close(); })();
